
import { createRoot } from 'react-dom/client';
import App from './App.tsx';  // Import from the TypeScript version
import './index.css';

createRoot(document.getElementById("root")).render(<App />);
